apiVersion: v1
kind: ConfigMap
metadata:
  name: magi-config
data:
  # The runtime derives its archetype (ADAM vs EVA) from MAGIS at boot;
  # no MAGI_NODE_ROLE env var is needed.
  MAGI_RELOAD: "1"
  MAGI_PORT: "8000"
  PYTHONPATH: /app
  VITE_BACKEND_URL: http://127.0.0.1:8000
