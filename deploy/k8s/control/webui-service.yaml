apiVersion: v1
kind: Service
metadata:
  name: magi-webui
  labels:
    app.kubernetes.io/name: magi
    app.kubernetes.io/component: webui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: magi
    app.kubernetes.io/component: webui
  ports:
    - name: http
      port: 42069
      targetPort: http
