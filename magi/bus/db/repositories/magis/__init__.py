"""MAGIS-PostgreSQL Repository classes — one per domain.

Each module in this package exposes a single Repository class that
encapsulates all SQLAlchemy access for its domain against the MAGIS
PG engine.  See :mod:`magi.bus.db.repositories` for the full
inventory.
"""
