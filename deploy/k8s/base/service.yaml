apiVersion: v1
kind: Service
metadata:
  name: magi
  labels:
    app.kubernetes.io/name: magi
    app.kubernetes.io/component: node
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: magi
    app.kubernetes.io/component: node
  ports:
    - name: http
      port: 42069
      targetPort: http
      protocol: TCP
