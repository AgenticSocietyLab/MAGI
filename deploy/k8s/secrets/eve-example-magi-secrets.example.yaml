# Example only. Replace every placeholder and apply this file locally.
# Do not commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: eva-example-magi-secrets
  namespace: magi
type: Opaque
stringData:
  MAGI_BOT_TOKEN: replace-with-this-evas-telegram-bot-token
  MAGI_SHARED_SECRET: replace-with-the-adam-shared-secret
