apiVersion: v1
kind: ConfigMap
metadata:
  name: magi-config
data:
  MAGI_NODE_ROLE: eve
  # MAGI_CHANNELS intentionally unset — Eve's role default is
  # ("telegram",); the daemon is brought up by the wizard (Eve
  # re-binds per-user during onboarding).
  MAGI_UID: eve-example
  MAGI_ADAM_URL: http://adam-magi:42069
