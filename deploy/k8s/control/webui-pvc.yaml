apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: magi-webui-workspace
  labels:
    app.kubernetes.io/name: magi
    app.kubernetes.io/component: webui
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
